// Service for interacting with the OpenAI API

const { generateContent } = require('./openai'); // Assuming the original function is in './openai.js'

// You might want to enhance this with more specific functions or error handling
// For now, just re-exporting the existing function

module.exports = {
  generateContent
}; 